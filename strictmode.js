function foo() {
	"use strict";	// включить строгий режим
	a = 1;			// `var` missing, ReferenceError
}

foo();


// Хотя это может показаться очевидным из синтаксиса,
// foo — по сути просто переменная во внешней окружающей области видимости,
// у которой есть ссылка на объявляемую функцию. 
// То есть, функция сама является значением, так же как 42 или [1,2,3].